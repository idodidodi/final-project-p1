apiVersion: v1
kind: Pod
metadata:
  name: flask-app
  labels:
    app: flask-app
spec:
  containers:
    - name: flask-app
      image: idodidodi/hello-flask:v1
      ports:
        - containerPort: 5000
